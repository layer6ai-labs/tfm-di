# Fast attack configuration for quick testing

name: fast
type: single

# TabDPT model parameters
model:
  n_ensembles: 2  # Minimal ensembles for speed
  temperature: 0.8
  context_size: 512  # Smaller context

# Attack-specific parameters
params:
  max_samples: 200  # Limit samples for speed
