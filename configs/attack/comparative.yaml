# Comparative attack configuration (query vs reference)

name: comparative
type: comparative

# TabDPT model parameters
model:
  n_ensembles: 4  # Fewer ensembles for speed when comparing
  temperature: 0.8
  context_size: 1024

# Attack-specific parameters
params:
  reference_dataset_id: 54  # Reference dataset (known non-member)
  max_samples: null
